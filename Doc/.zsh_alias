# aliases
alias reloadzsh="exec zsh && source ~/.zshrc"

alias psn="ps aux | grep node -w"
alias psm="ps aux | grep mongod -w"
alias psr="ps aux | grep redis-server -w"
alias mtl="multitail ~/.ssh/srv-log/error.log ~/.ssh/srv-log/running.log.*"
alias psmr="psm; psr"
alias ses="EGG_SERVER_ENV=test npm run dev"
alias nrd="npm run dev"
alias ut="EGG_SERVER_ENV=local npm run test-local"
alias mu="npm run migrate:up"

alias n="node"
alias nv="node -v"

alias cdi="cd ~/Bitbucket/iot-ble-projects/services "

alias cdp="~/Workspace/Bitbucket/itim-zygl-bom/projects/bom/server/portal"
alias cde="cd ~/Exercise"
alias cdt="cd $HOME/GitHub/Exercise/TensorFlow"
alias cdnx="cd /usr/local/etc/nginx"

alias sshvm="ssh will@UbuntuVM"
alias sshdev="ssh slgdev@10.12.7.93"

alias ....="cd ../../.."
alias .....="cd ../../../.."
alias cd-="cd -"
alias l="ls"
alias la="ls -lah"
alias ll="la"
alias lm='ll | more'
alias lsd='ls -d *(/)'
alias df='df -h'
alias du='du -h -d1'
alias cp='cp -iR'
# alias cat='cat -n'

alias mv='mv -i'
alias rm='rm -ivr'
alias md='mkdir -p'
alias ztar='tar -czvf'
alias uztar='tar -xzvf'
alias ubz2tar='tar -jvxf'
alias ttar='tar -tvf'
alias man='man -a'
alias vi='vim'
alias mvi='gvim'

alias gitk='/opt/homebrew/Cellar/git-gui/2.43.0/bin/gitk'
alias gk='gitk'
alias ga='git add --ignore-removal'
alias gsu='git submodule update'
alias gcz='git add .; git cz'


# Example aliases
# alias zshconfig="mate ~/.zshrc"
# alias ohmyzsh="mate ~/.oh-my-zsh"

alias rc='redis-cli'

# alias myip='ifconfig | grep inet\ '

alias j='java'
alias mp='mvn package'
alias jsh='/Library/Java/JavaVirtualMachines/jdk-9.jdk/Contents/Home/bin/jshell'
# alias psync='cdp; fis3 release lzg -w'


# alias rmongo='cd /tmp; mkdir -p /tmp/log; sleep 1; pkill mongo; sleep 2; rm -rf /data/db/mongod.lock; sleep 2; mongod --dbpath /data/db --rest --httpinterface > /tmp/log/mongo.log&; sleep 1; cd -'
# alias rmongo='cd /tmp; mkdir -p /tmp/log; sleep 1; pkill mongo; sleep 2; rm -rf /data/db/mongod.lock; sleep 2; mongod --config /usr/local/etc/mongod.conf --dbpath /data/db > /tmp/log/mongo.log&; sleep 1; cd -'
alias rmongo='cd /tmp; mkdir -p /tmp/log; sleep 1; pkill mongo; sleep 2; rm -rf /data/db/mongod.lock; sleep 2; mongod --dbpath /data/db > /tmp/log/mongo.log&; sleep 1; cd -'
# alias rredis='cd /tmp; mkdir -p /tmp/log; sleep 1; pkill redis; sleep 2; redis-server > /tmp/log/redis.log&; sleep 1; cd -'
alias rredis='pushd /opt/redis/current/bin; ./boot-redis-cluster.sh; popd'
# alias rdb='rmongo; sleep 2; rredis;'
alias rdb='rredis'
alias sdb='rdb'

alias rmysql='pushd /opt/mysql-8.0.22; ./boot-mysqld.sh; popd'

# alias f='nvm use v8.16.1; sleep 0.5; stf local'
alias f='nvm use; sleep 0.5; stf local'
alias ft='cd /var/folders/45/86qtmmt925x9wbsdqdgxnh7r0000gn/T'

alias dk='docker'
alias dkc='docker-compose'
alias tailf='tail -f'
alias tf='tail -f'
alias sls='screen -ls'
alias sr='screen -R'
alias py='python3'
# alias python='python3'
alias am='aws-mfa --force --token'
alias k='kubectl'
alias kex='k exec -itn'  # k web-srv-ns pod/srv-8bid -- sh

alias m='make'
alias getesp8266="export PATH=$HOME/esp/xtensa-lx106-elf/bin:$PATH; export IDF_PATH=$HOME/esp/ESP8266_RTOS_SDK"
alias getidf='. $HOME/esp/esp-idf/export.sh'
alias i='idf.py'
alias ib='i build'
alias im='i menuconfig'
alias ia='i -p /dev/cu.wchusbserial14210 build flash monitor'
alias ifm='i -p /dev/cu.wchusbserial14210 flash monitor'

alias cf='cargo fmt'
alias cck='cargo check'
alias cb='cargo build'
alias cbr='cargo build --release'
alias cr='cargo run'
alias crr='cargo run --release'

